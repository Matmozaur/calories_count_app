BoxLayout:
    orientation: 'vertical'
    Label:
        id: result_label
        text: "Press the button and speak"
        size_hint: (1, 0.8)
    Button:
        text: "Start Recording"
        size_hint: (1, 0.2)
        on_press: app.record_speech()
